import { Text, View } from "react-native";

export default function UserAuthPage() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is the user auth page</Text>
    </View>
  );
}
